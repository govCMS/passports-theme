/**
 * @file
 * Empty javascript file for the purpose of removing the parent theme script.js.
 *
 * Because including a file of the same name will override the previously added
 * file, by including an empty file we essentially stop the other version of the
 * file from being added, which we're doing here because we don't want the JS
 * functionality added by the govcms_zen theme.
 *
 * For the actual main script file for this theme see js/passports.js.
 */

